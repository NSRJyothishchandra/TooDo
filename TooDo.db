using System;
using System.IO;
using SQLite;

namespace TooDo
{
    public static class DatabaseHelper
    {
        public static SQLiteConnection CreateConnection()
        {
            string databasePath = Path.Combine(Environment.CurrentDirectory, "TooDo.db");
            return new SQLiteConnection(databasePath);
        }
    }
}
